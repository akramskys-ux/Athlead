# Session 2026-06-07-01

## Objective
Arrancar construcción del MVP. Definir división de trabajo (Claude = cabeza/arquitecto, Antigravity = manos/constructor) y construir los primeros bloques de infraestructura.

## Scope
- Validación de scope del MVP (vía Antigravity como crítico de producto).
- Bloque 1: capa Room (`core-database`).
- Preparación estructural para Bloque 2: theme D1 (`core-ui`).

## Decisions
- **Modelo de trabajo**: Claude (Opus) define arquitectura, specs exactas, esqueletos, revisa y dirige. Antigravity (Gemini) escribe la implementación siguiendo specs. Antigravity NO toma decisiones de arquitectura.
- **Auth MVP**: Email/Password + "Recordarme" (sesión persistente). Google Sign-In → post-MVP.
- **Paquete**: `com.athlead` (confirmado, ya en build files).
- **`coachId: String`** agregado a las 6 entidades sincronizables (no en join tables ni MatchEvent). RLS fuera del MVP; se filtra por coachId en queries locales.
- **Partidos**: play-by-play (`MatchEventEntity`) SÍ incluido (local-only).
- **Calendario reordenado** (propuesta Antigravity aprobada): S1 setup+DB+theme, S2 auth+Supabase, S3 roster+métricas, S4 sesiones+tiros, S5 vueltas+partidos, S6 sync, S7 rankings+dashboard, S8 polish.
- **Fuentes D1**: estáticas (no variable) por minSdk 24, descargadas de @expo-google-fonts (OFL).

## Files changed
- `gradle/libs.versions.toml`: + kotlinx-datetime 0.6.2 (version + library).
- `core-database/build.gradle.kts`: + kotlinx-datetime dep, + ksp room.schemaLocation.
- `core-database/src/main/java/com/athlead/core/database/**`: 24 archivos (9 entities, 7 DAOs, 3 models, 2 relations, Converters, AthleedDatabase) — construido por Antigravity.
- `core-ui/src/main/res/font/`: 7 .ttf (archivo_extrabold, hanken_grotesk_{regular,medium,semibold,bold}, ibm_plex_mono_{regular,medium}).

## Tests
- `:core-database:compileDebugKotlin` → BUILD SUCCESSFUL. Tests unitarios de DAOs pendientes (bloque futuro).

## Risks
- Pull-sync (restauración en nuevo dispositivo) aún no implementado — pendiente S6.
- Orden topológico de sync + refresh de token JWT en SyncWorker — pendiente S6.
- Fuentes Hanken Grotesk/Archivo: verificar render real en emulador cuando haya UI.

## Progreso de bloques (Semana 1 — COMPLETA)
- Bloque 1 ✅ core-database: 9 entities, 7 DAOs, 3 models, 2 relations, Converters, AthleedDatabase. coachId + soft cascade delete. Compila.
- Bloque 2 ✅ core-ui: theme D1 (Color, Type, Spacing, Theme) + 7 componentes base. + material-icons-extended agregado por Claude. Compila.
- Bloque 3 ✅ app shell: DatabaseModule (Hilt en core-database), AthleadApplication (@HiltAndroidApp), MainActivity (@AndroidEntryPoint + AthleedTheme). APK debug generado OK.

## Pendientes técnicos anotados (no bloquean)
- AthleedButton variant Secondary no dibuja borde (usar OutlinedButton al construir 1ra pantalla con botón secundario).
- ArrowBackIcon dibujado a mano en StandardScreenLayout: reemplazar por Icons.AutoMirrored.Filled.ArrowBack (ya hay material-icons-extended).

## CAMBIO DE DECISIÓN (Semana 2) — Auth: Firebase → Supabase
- **Se reemplaza Firebase Auth por Supabase Auth.** Razón: un solo backend, RLS nativo con auth.uid(), sin puente JWT Firebase↔Supabase (riesgo eliminado). coachId = auth.uid().
- RLS: prueba de concepto AHORA (tabla athletes + política RLS) para validar auth+RLS end-to-end.
- Stack: SDK supabase-kt 3.1.4 (auth-kt + postgrest-kt), multiplatform-settings-no-arg 1.3.0 (persistencia de sesión), datastore-preferences 1.1.1 (flag Recordarme). Firebase eliminado del catálogo y de core-auth. Plugin google-services eliminado.
- Arquitectura: core-network = dueño del SupabaseClient (Auth+Postgrest) + llaves vía BuildConfig (SUPABASE_URL/SUPABASE_ANON_KEY desde local.properties). core-auth depende de core-network. core-data usa postgrest.
- Verificado: :core-network/:core-auth/:core-data compilan con supabase-kt 3.1.4.
- PENDIENTE doc: actualizar CLAUDE.md, AGENTS.md, decision-log.md (revierte ADR Firebase).

## Follow-up
- Usuario: crear proyecto Supabase, correr SQL athletes+RLS, desactivar email confirmation, dar URL+anon key.
- Bloque 4: core-network SupabaseClient + core-auth SupabaseAuthManager + AuthRepository (Recordarme).
- Bloque 5: feature-auth (Login/Registro/Reset). Bloque 6: navegación condicional. PoC: athletes remoto vía postgrest.

## Suggested commit grouping
1. `chore(deps): add kotlinx-datetime + room schema export config`
2. `feat(core-database): add Room entities, DAOs and database (coachId + soft cascade delete)`
3. `chore(core-ui): add D1 font resources`
